# UATVR runtime and development dependencies.
#
# Target platform: Linux x86_64, Python 3.10 or 3.11, NVIDIA CUDA.
# The CUDA 12.1 wheels below work with the host's CUDA 12.3 driver and, unlike
# the former PyTorch 1.13 / torchvision 0.14 combination, provide CPython 3.11
# wheels. Install with: python -m pip install -r requirements.txt
--extra-index-url https://download.pytorch.org/whl/cu121

# Core training and video preprocessing
torch==2.1.2+cu121
torchvision==0.16.2+cu121
numpy==1.26.0
Pillow==10.0.1
opencv-python==4.8.1.78

# CLIP tokenization, model utilities, and pretrained-weight downloads
ftfy==6.1.1
regex==2022.7.9
tqdm==4.65.0
requests==2.31.0
boto3==1.28.85
einops==0.7.0
scikit-learn==1.3.2

# Tests and repository checks
pytest==7.1.2
ruff==0.6.9

# Intentionally not installed by default:
# - mamba-ssm: required only by inactive legacy Mamba modules; it needs a
#   CUDA/compiler build matching the installed PyTorch version.
