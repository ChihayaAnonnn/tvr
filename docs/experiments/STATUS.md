# STATUS — 当前实验状态（更新于 2026-08-01）

<!-- 覆盖写，≤40 行（exp_context.sh 会检查并提醒）。只描述"现在"，历史归 JOURNAL.md
     和 baseline-gap-diagnosis.md。一步实验收尾时更新；SessionStart / PostCompact 自动注入。 -->

## 在追的问题
条件覆盖有**两个正交的轴**，诊断相反：
- **轴二 层内难度**（14.0 pt）：校准层问题，**已解决**。F 里加一个桶内中心化的难度
  标量 → 3.2–5.1 pt，1.2 倍集合，全免费特征。oracle |Rel| 对它的转移是零。
- **轴一 歧义**（15.0 pt）：信号层问题。校准层穷尽在 11.4–14.6；外部 VLM 判官把它
  推到 **9.4–9.8**，但 oracle 是 0.7。**残余 10.7 pt 只补上 19%**；两轴分开打。

## 上一步结论（step-004，Qwen2.5-VL-7B 逐对判 yes/no，64,433 对）
ρ 主判据不过：可部署软计数 **0.670**，加校准+融合 0.713，要 0.75（减半要 0.83）。
**但失败被完全归因到判官容量**：池子天花板 0.972；只把 top-30 里 FIRE 判过的
**29%** 换成人工标签，ρ 就从 0.690 跳到 **0.878**；逐对 AUC 0.924 但 TPR 只有 0.505。
spread 上是真的但小：对 `GCC +diff` **Δsprd_rel −2.0 pt，4/4 seed 显著，集合 1.0 倍**。
**代价不匹配**：这 2 pt 要每查询 50 次 7B 前向，测试期方案站不住，论文要自己说。
真实判官 ρ=0.67 给 9.6，噪声 oracle 插值预测 9.0——**真实预测器第二次低于同 ρ 曲线**。

## 正在跑：无

## 下一步
1. **把判官挪到训练期**（倾向这条）：训练集离线判，蒸馏便宜的歧义头，测试期零成本。
   天花板已知：完美蒸馏 7B 判官 = ρ 0.67 = 9.6 pt。
2. 换判官容量（32B/72B）是归因指的变量，但成本问题只会更糟，即使成功。
3. 跨任务复现（COCO 文本-图像，天然多正例，只需相似度矩阵）> 多刷 TVR backbone。

## 已排除的死路（不要重开）
- **DUA 家族整体**：八臂两协议没一个超过 +0.4，复现误差 1.1；σ 三种用法全负。
- **靠换校准层闭合轴一**：Mondrian / CQR / GCC 三代，全部 11.4–14.6。
- **调判官的 prompt / 阈值 / K**：K 从 10 到 50 只动 0.65–0.69，校准+融合只 +0.04。
- **per-bucket 交互项**；**17 特征喂一个合并的 λ(x)**；**自适应 margin 集合**。

## 长期约束
- 待超基线是 **`GCC +diff VLM`**（sprd_rel 9.6 / sprd_dif 5.6 / 集合 6.3）。
- **任何预测器只按 spread 打分，绝不按 ρ**——真实预测器两次都低于同 ρ 的噪声 oracle。
- 两轴真天花板在 n=1000 上**测不出来**（25 格 × 20 点太薄）——limitation 要写。
- spread 用**配对 bootstrap**；同时报原始/FIRE 标签，两臂比较用配对 McNemar；
  `any` 覆盖是下界；判官帧缓存在 `.scratch/grid_frames`，P(yes) 在 `.scratch/vlm_judge_full`。
