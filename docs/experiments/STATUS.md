# STATUS — 当前实验状态（更新于 2026-07-31）

<!-- 覆盖写，≤40 行（exp_context.sh 会检查并提醒）。只描述"现在"，历史归 JOURNAL.md
     和 baseline-gap-diagnosis.md。一步实验收尾时更新；SessionStart / PostCompact 自动注入。 -->

## 在追的问题
条件覆盖有**两个正交的轴**，step-003 测出两者的诊断**相反**：
- **轴一 歧义**（按真实 |Rel| 分层，15.0 pt）：**信号层问题**。所有校准变体都卡在
  11.4–14.6，oracle 只要 0.4–0.8。**校准层已穷尽**，换函数类没用。
- **轴二 层内难度**（按桶内难度五分位，14.0 pt）：**校准层问题，已基本解决**。
  在 F 里加**一个**桶内中心化的难度标量 → 3.2–5.1 pt，只花 1.2 倍集合，全免费特征。
- **oracle |Rel| 对轴二的转移是零**（sprd_dif 15.2，global 15.0）。「估准 |Rel|
  就解决了」已被证伪——两个轴要分开打。

## 上一步结论（step-003，GCC 条件 conformal）
主判据不过（Δsprd_rel +0.19~+1.53，要 ≤−2.0），但诊断判据给了明确答案（见上）。
正确性判据过：`GCC buckets` 对 Mondrian 差 ≤0.8 pt。**交互项是我对 step-002 的
过度推论**：per-bucket 斜率四个 run、两个轴上全不如共享斜率，250 行分五桶在 90
分位上尾部只剩 5 个点。**效应在 n=1000 真实、在 n=250 不可估**。
有限样本版（+∞ 填补）要 2.3–2.8 倍集合并过覆盖到 91%——正文用渐近版。

## 正在跑：无

## 下一步
1. **外部判官估 |Rel|**（轴一唯一剩下的路）。双塔召回 top-50，VLM 逐个判 yes/no，
   命中数当估计。先在 200 个查询上测 Spearman，**ρ 达不到 0.75 就停**（要减半需 0.83）。
2. 跨任务复现（COCO 文本-图像，天然多正例，只需相似度矩阵）> 多刷 TVR backbone。

## 已排除的死路（不要重开）
- **DUA 家族整体**：八臂两协议没一个超过 +0.4，复现误差 1.1；σ 三种用法全负。
- **靠换校准层闭合轴一**：Mondrian / CQR / GCC 三代都试过，全部 11.4–14.6。
- **per-bucket 交互项**：四个 run 两个轴全负（见上）。
- **17 个特征喂给一个合并的 λ(x)**：形式上不成立，层内用还会让集合涨 4 倍。
- **自适应 margin 集合**优于 top-k；**GBR 分位数回归**；**标签错误腐蚀 risk–coverage**。

## 长期约束
- 待超基线是 **`GCC +diff`**（轴二接近天花板），不再是 `bucket rich`。
- 两轴真天花板在 n=1000 上**测不出来**（25 格 × 20 点太薄）——limitation 要写。
- spread 用**配对 bootstrap**；同时报原始/FIRE 标签，两臂比较用配对 McNemar；
  `any` 覆盖是下界（集合里未被判断的相关视频不计入）。
